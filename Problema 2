#include <stdio.h>
#include <stdlib.h>

int n,i,j,a[100][100];
int main()
{
scanf("%d",&n);
for(i=1;i<=n;i++)
for(j=1;j<=n;j++)
{
if(i%2!=0)
a[i][j]=1;
else a[i][j]=j;
}
for(i=1;i<=n;i++)
{
for(j=1;j<=n;j++)
printf("%d",a[i][j]);

printf("\n");
}
}
